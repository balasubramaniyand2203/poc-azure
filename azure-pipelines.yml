trigger:
  branches:
    include:
      - feature/app-deployment

pool:
  name: 'bala-poc'

variables:
  buildConfiguration: 'Release'
  azureSubscription: 'aks-dev'
  aksclustername: "dev-bcc-aks"
  k8sNamespace: 'default'
  imagePullSecretName: 'acr-pull'
  acrLoginServer: 'bala123reg.azurecr.io'
  # UPDATED: This must match exactly the name in your screenshot
  dockerRegistryServiceConnection: 'docker-dev' 
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: 'Build and Push Stage'
  jobs:
  - job: BuildJob
    steps:
    - task: UseDotNet@2
      inputs:
        packageType: 'sdk'
        version: '8.x'

    - task: Docker@2
      displayName: 'Build and Push API'
      inputs:
        containerRegistry: '$(dockerRegistryServiceConnection)' # Uses docker-dev
        repository: 'server-api'
        command: 'buildAndPush'
        Dockerfile: 'server.api/Dockerfile'
        buildContext: '$(Build.SourcesDirectory)'
        tags: |
          $(tag)

    - task: Docker@2
      displayName: 'Build and Push Client'
      inputs:
        containerRegistry: '$(dockerRegistryServiceConnection)' # Uses docker-dev
        repository: 'client-app'
        command: 'buildAndPush'
        Dockerfile: 'client-app/Dockerfile'
        buildContext: '$(Build.SourcesDirectory)'
        tags: |
          $(tag)

    - publish: $(System.DefaultWorkingDirectory)/deployment.yaml
      artifact: manifests

- stage: Deploy
  displayName: 'Deploy to Dev'
  dependsOn: Build
  jobs:
  - job: DeployToAKS
    steps:
    - download: current
      artifact: manifests

    - task: KubernetesManifest@1
      displayName: 'Create ACR imagePullSecret'
      inputs:
        action: 'createSecret'
        kubernetesServiceConnection: '$(azureSubscription)'
        namespace: '$(k8sNamespace)'
        secretName: '$(imagePullSecretName)'
        dockerRegistryEndpoint: '$(dockerRegistryServiceConnection)' # Uses docker-dev

    - task: KubernetesManifest@1
      displayName: 'Deploy to AKS'
      inputs:
        action: 'deploy'
        kubernetesServiceConnection: '$(azureSubscription)'
        namespace: '$(k8sNamespace)'
        manifests: |
          $(Pipeline.Workspace)/manifests/deployment.yaml
        imagePullSecrets: |
          $(imagePullSecretName)
        containers: |
          $(acrLoginServer)/server-api:$(tag)
          $(acrLoginServer)/client-app:$(tag)