trigger:
  branches:
    include:
      - feature/app-deployment

pool:
  name: 'bala-poc'

variables:
  buildConfiguration: 'Release'
  azureSubscription: 'aks-dev'
  aksclustername: "dev-bcc-aks"
  k8sNamespace: 'default'
  imagePullSecretName: 'acr-pull'
  acrLoginServer: 'bala123reg.azurecr.io'
  dockerRegistryServiceConnection: 'docker-dev' # Matches your screenshot
  tag: '$(Build.BuildId)'

stages:
  - stage: Build
    displayName: 'Build and Push Stage'
    jobs:
      - job: BuildJob
        displayName: 'Build and Push Docker Image'
        steps:
          - task: UseDotNet@2
            inputs:
              packageType: 'sdk'
              version: '8.x'

          - task: Docker@2
            displayName: 'Build and Push API'
            inputs:
              containerRegistry: '$(dockerRegistryServiceConnection)'
              repository: 'server-api'
              command: 'buildAndPush'
              # Fixed path based on your 'ls' command
              Dockerfile: 'src/server-api/Dockerfile' 
              buildContext: '$(Build.SourcesDirectory)'
              tags: |
                $(tag)

          - task: Docker@2
            displayName: 'Build and Push Client'
            inputs:
              containerRegistry: '$(dockerRegistryServiceConnection)'
              repository: 'client-app'
              command: 'buildAndPush'
              # Adjusting to src/ path as well
              Dockerfile: 'src/client-app/Dockerfile' 
              buildContext: '$(Build.SourcesDirectory)'
              tags: |
                $(tag)

          # Move the manifest to the next stage
          - publish: $(System.DefaultWorkingDirectory)/deployment.yaml
            artifact: manifests

  - stage: Deploy
    displayName: 'Deploy to Dev'
    dependsOn: Build
    jobs:
      - job: DeployToAKS
        steps:
          # Download the manifest published in Build
          - download: current
            artifact: manifests

          - task: KubernetesManifest@1
            displayName: 'Create ACR imagePullSecret'
            inputs:
              action: 'createSecret'
              kubernetesServiceConnection: '$(azureSubscription)'
              namespace: '$(k8sNamespace)'
              secretName: '$(imagePullSecretName)'
              dockerRegistryEndpoint: '$(dockerRegistryServiceConnection)'

          - task: KubernetesManifest@1
            displayName: 'Deploy to AKS'
            inputs:
              action: 'deploy'
              kubernetesServiceConnection: '$(azureSubscription)'
              namespace: '$(k8sNamespace)'
              manifests: |
                $(Pipeline.Workspace)/manifests/deployment.yaml
              imagePullSecrets: |
                $(imagePullSecretName)
              containers: |
                $(acrLoginServer)/server-api:$(tag)
                $(acrLoginServer)/client-app:$(tag)